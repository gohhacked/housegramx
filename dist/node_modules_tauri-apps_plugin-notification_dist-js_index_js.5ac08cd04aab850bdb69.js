"use strict";
(self["webpackChunkhousegramx"] = self["webpackChunkhousegramx"] || []).push([["node_modules_tauri-apps_plugin-notification_dist-js_index_js"],{

/***/ "./node_modules/@tauri-apps/plugin-notification/dist-js/index.js":
/*!***********************************************************************!*\
  !*** ./node_modules/@tauri-apps/plugin-notification/dist-js/index.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Importance: () => (/* binding */ Importance),
/* harmony export */   Schedule: () => (/* binding */ Schedule),
/* harmony export */   ScheduleEvery: () => (/* binding */ ScheduleEvery),
/* harmony export */   Visibility: () => (/* binding */ Visibility),
/* harmony export */   active: () => (/* binding */ active),
/* harmony export */   cancel: () => (/* binding */ cancel),
/* harmony export */   cancelAll: () => (/* binding */ cancelAll),
/* harmony export */   channels: () => (/* binding */ channels),
/* harmony export */   createChannel: () => (/* binding */ createChannel),
/* harmony export */   isPermissionGranted: () => (/* binding */ isPermissionGranted),
/* harmony export */   onAction: () => (/* binding */ onAction),
/* harmony export */   onNotificationReceived: () => (/* binding */ onNotificationReceived),
/* harmony export */   pending: () => (/* binding */ pending),
/* harmony export */   registerActionTypes: () => (/* binding */ registerActionTypes),
/* harmony export */   removeActive: () => (/* binding */ removeActive),
/* harmony export */   removeAllActive: () => (/* binding */ removeAllActive),
/* harmony export */   removeChannel: () => (/* binding */ removeChannel),
/* harmony export */   requestPermission: () => (/* binding */ requestPermission),
/* harmony export */   sendNotification: () => (/* binding */ sendNotification)
/* harmony export */ });
/* harmony import */ var _tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @tauri-apps/api/core */ "./node_modules/@tauri-apps/api/core.js");


// Copyright 2019-2023 Tauri Programme within The Commons Conservancy
// SPDX-License-Identifier: Apache-2.0
// SPDX-License-Identifier: MIT
/**
 * Send toast notifications (brief auto-expiring OS window element) to your user.
 * Can also be used with the Notification Web API.
 *
 * @module
 */
var ScheduleEvery;
(function (ScheduleEvery) {
    ScheduleEvery["Year"] = "year";
    ScheduleEvery["Month"] = "month";
    ScheduleEvery["TwoWeeks"] = "twoWeeks";
    ScheduleEvery["Week"] = "week";
    ScheduleEvery["Day"] = "day";
    ScheduleEvery["Hour"] = "hour";
    ScheduleEvery["Minute"] = "minute";
    /**
     * Not supported on iOS.
     */
    ScheduleEvery["Second"] = "second";
})(ScheduleEvery || (ScheduleEvery = {}));
class Schedule {
    static at(date, repeating = false, allowWhileIdle = false) {
        return {
            at: { date, repeating, allowWhileIdle },
            interval: undefined,
            every: undefined
        };
    }
    static interval(interval, allowWhileIdle = false) {
        return {
            at: undefined,
            interval: { interval, allowWhileIdle },
            every: undefined
        };
    }
    static every(kind, count, allowWhileIdle = false) {
        return {
            at: undefined,
            interval: undefined,
            every: { interval: kind, count, allowWhileIdle }
        };
    }
}
var Importance;
(function (Importance) {
    Importance[Importance["None"] = 0] = "None";
    Importance[Importance["Min"] = 1] = "Min";
    Importance[Importance["Low"] = 2] = "Low";
    Importance[Importance["Default"] = 3] = "Default";
    Importance[Importance["High"] = 4] = "High";
})(Importance || (Importance = {}));
var Visibility;
(function (Visibility) {
    Visibility[Visibility["Secret"] = -1] = "Secret";
    Visibility[Visibility["Private"] = 0] = "Private";
    Visibility[Visibility["Public"] = 1] = "Public";
})(Visibility || (Visibility = {}));
/**
 * Checks if the permission to send notifications is granted.
 * @example
 * ```typescript
 * import { isPermissionGranted } from '@tauri-apps/plugin-notification';
 * const permissionGranted = await isPermissionGranted();
 * ```
 *
 * @since 2.0.0
 */
async function isPermissionGranted() {
    if (window.Notification.permission !== 'default') {
        return await Promise.resolve(window.Notification.permission === 'granted');
    }
    return await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|is_permission_granted');
}
/**
 * Requests the permission to send notifications.
 * @example
 * ```typescript
 * import { isPermissionGranted, requestPermission } from '@tauri-apps/plugin-notification';
 * let permissionGranted = await isPermissionGranted();
 * if (!permissionGranted) {
 *   const permission = await requestPermission();
 *   permissionGranted = permission === 'granted';
 * }
 * ```
 *
 * @returns A promise resolving to whether the user granted the permission or not.
 *
 * @since 2.0.0
 */
async function requestPermission() {
    return await window.Notification.requestPermission();
}
/**
 * Sends a notification to the user.
 * @example
 * ```typescript
 * import { isPermissionGranted, requestPermission, sendNotification } from '@tauri-apps/plugin-notification';
 * let permissionGranted = await isPermissionGranted();
 * if (!permissionGranted) {
 *   const permission = await requestPermission();
 *   permissionGranted = permission === 'granted';
 * }
 * if (permissionGranted) {
 *   sendNotification('Tauri is awesome!');
 *   sendNotification({ title: 'TAURI', body: 'Tauri is awesome!' });
 * }
 * ```
 *
 * @since 2.0.0
 */
function sendNotification(options) {
    if (typeof options === 'string') {
        new window.Notification(options);
    }
    else {
        new window.Notification(options.title, options);
    }
}
/**
 * Register actions that are performed when the user clicks on the notification.
 *
 * @example
 * ```typescript
 * import { registerActionTypes } from '@tauri-apps/plugin-notification';
 * await registerActionTypes([{
 *   id: 'tauri',
 *   actions: [{
 *     id: 'my-action',
 *     title: 'Settings'
 *   }]
 * }])
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function registerActionTypes(types) {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|register_action_types', { types });
}
/**
 * Retrieves the list of pending notifications.
 *
 * @example
 * ```typescript
 * import { pending } from '@tauri-apps/plugin-notification';
 * const pendingNotifications = await pending();
 * ```
 *
 * @returns A promise resolving to the list of pending notifications.
 *
 * @since 2.0.0
 */
async function pending() {
    return await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|get_pending');
}
/**
 * Cancels the pending notifications with the given list of identifiers.
 *
 * @example
 * ```typescript
 * import { cancel } from '@tauri-apps/plugin-notification';
 * await cancel([-34234, 23432, 4311]);
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function cancel(notifications) {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|cancel', { notifications });
}
/**
 * Cancels all pending notifications.
 *
 * @example
 * ```typescript
 * import { cancelAll } from '@tauri-apps/plugin-notification';
 * await cancelAll();
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function cancelAll() {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|cancel');
}
/**
 * Retrieves the list of active notifications.
 *
 * @example
 * ```typescript
 * import { active } from '@tauri-apps/plugin-notification';
 * const activeNotifications = await active();
 * ```
 *
 * @returns A promise resolving to the list of active notifications.
 *
 * @since 2.0.0
 */
async function active() {
    return await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|get_active');
}
/**
 * Removes the active notifications with the given list of identifiers.
 *
 * @example
 * ```typescript
 * import { cancel } from '@tauri-apps/plugin-notification';
 * await cancel([-34234, 23432, 4311])
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function removeActive(notifications) {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|remove_active', { notifications });
}
/**
 * Removes all active notifications.
 *
 * @example
 * ```typescript
 * import { removeAllActive } from '@tauri-apps/plugin-notification';
 * await removeAllActive()
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function removeAllActive() {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|remove_active');
}
/**
 * Creates a notification channel.
 *
 * @example
 * ```typescript
 * import { createChannel, Importance, Visibility } from '@tauri-apps/plugin-notification';
 * await createChannel({
 *   id: 'new-messages',
 *   name: 'New Messages',
 *   lights: true,
 *   vibration: true,
 *   importance: Importance.Default,
 *   visibility: Visibility.Private
 * });
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function createChannel(channel) {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|create_channel', { ...channel });
}
/**
 * Removes the channel with the given identifier.
 *
 * @example
 * ```typescript
 * import { removeChannel } from '@tauri-apps/plugin-notification';
 * await removeChannel();
 * ```
 *
 * @returns A promise indicating the success or failure of the operation.
 *
 * @since 2.0.0
 */
async function removeChannel(id) {
    await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|delete_channel', { id });
}
/**
 * Retrieves the list of notification channels.
 *
 * @example
 * ```typescript
 * import { channels } from '@tauri-apps/plugin-notification';
 * const notificationChannels = await channels();
 * ```
 *
 * @returns A promise resolving to the list of notification channels.
 *
 * @since 2.0.0
 */
async function channels() {
    return await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.invoke)('plugin:notification|listChannels');
}
async function onNotificationReceived(cb) {
    return await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.addPluginListener)('notification', 'notification', cb);
}
async function onAction(cb) {
    return await (0,_tauri_apps_api_core__WEBPACK_IMPORTED_MODULE_0__.addPluginListener)('notification', 'actionPerformed', cb);
}




/***/ })

}]);
//# sourceMappingURL=node_modules_tauri-apps_plugin-notification_dist-js_index_js.5ac08cd04aab850bdb69.js.map